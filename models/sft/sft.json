{
    "dataset_path": "../../datasets/sft/dataset.json",
    "tokenizer_path": "../tokenizer.json",
    "max_length": 1024,
    "model_dim": 1024,
    "num_heads": 16,
    "num_layers": 20,
    "finetune_layers": 4,
    "?finetune_layers": "为防止显存爆炸，只微调前8层参数。如果有足够显存则可去掉这一行。",
    "dropout": 0.1,
    "batch_size": 1,
    "val_batch_size": 0,
    "n_batches_per_step": 128,
    "max_learning_rate": 3e-4,
    "min_learning_rate": 1e-6,
    "warmup_steps": 0,
    "total_steps": 2000,
    "validation_interval": 50,

    "checkpoint_file": "checkpoint_pretrained_2.2971.pt",
    "checkpoint_step": 0,

    "log_file": "sft.log"
}